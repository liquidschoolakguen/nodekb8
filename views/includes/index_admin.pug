.card-body
    .col-md-12
        br 
        h7 Schulname | Plz | Ort          
    .col-md-12 
        h5.text-info #{school.name} | #{school.plz} | #{school.ort}
        a.btn.btn-info.text-white(href='/schools/edit1/'+ school._id) Stammdaten ändern
        br
        br
    .col-md-12
        br 
        h7 Adminschlüssel | Lehrerschlüssel | Schülerschlüssel          
    .col-md-12 
        .box
            a.text-info(href='/schools/edit_admin_schluessel', role='button') | #{school.admin_schluessel} &nbsp;
        .box
            a.text-info(href='/schools/edit_lehrer_schluessel', role='button') | #{school.lehrer_schluessel} &nbsp;
        .box
            a.text-info(href='/schools/edit_schueler_schluessel', role='button') | #{school.schueler_schluessel} &nbsp; 
        a.btn.btn-info.text-white(href='/schools/edit2/'+ school._id) Schulschlüssel ändern
        br
        br
    .form-group.col-md-12                                   
        each stamm, i in stamms
            .box
                a.text-info(href='/schools/stamms/' + stamm._id, role='button') | #{stamm.name} &nbsp;
        div(style='clear:both;') 
        a.btn.btn-info.text-white(href='/schools/add_single_stamm') Klassen hinzufügen 
        br
        br
    .form-group.col-md-12                                   
        each disziplin, i in disziplins
            .box
                a.text-info(href='/schools/disziplins/' + disziplin._id, role='button') | #{disziplin.name} &nbsp;
        div(style='clear:both;') 
        a.btn.btn-info.text-white(href='/schools/add_single_disziplin') Unterrichtsfach hinzufügen
        br
        br
    .form-group.col-md-12                                   
        each stammverbund, i in stammverbunds
            .box
                a.text-info(href='/schools/stammverbunds/' + stammverbund._id, role='button') | #{stammverbund.name} &nbsp;
        div(style='clear:both;') 
        a.btn.btn-info.text-white(href='/schools/add_stammverbund') Klassenverbund hinzufügen
        br
        br